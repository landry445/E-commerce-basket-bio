# --- Base de données (runtime app_user) ---
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=db_name
DB_USER=app_user
DB_PASSWORD=CHANGE_ME_app
DB_SSL=false

# --- Connexion pour migrations (migration_user) ---
MIGRATION_DB_USER=migration_user
MIGRATION_DB_PASSWORD=CHANGE_ME_mig

# --- JWT & Cookies ---
JWT_SECRET=CHANGE_ME_super_secret_min_32_chars
JWT_EXPIRES_IN=7d
COOKIE_NAME=auth_token
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax

# --- CORS ---
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true
